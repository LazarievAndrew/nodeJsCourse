const info = (message) => {
    console.info(message);
}

const error = (message) => {
    console.error(message);
}

const warn = (message) => {
    console.warn(message);
}

module.exports = {
    info,
    error,
    warn,
}